#include <iostream>
#include <string>
#include <algorithm>
#include <vector>

using namespace std;

int main() {
  int n, i, j;
  scanf("%d", &n);

  pair<int, int> alpha_degrees[26];
  vector<string> words;
  for(i = 0; i < n; i++) {
    string word;
    cin >> word;
    words.push_back(word);
    if(words)
  }

  return 0;
}